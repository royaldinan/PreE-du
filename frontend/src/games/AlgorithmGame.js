Sekarang saya akan membuat 3 game untuk Track Computational Thinking dalam satu batch:
Action: {"type": "mcp_tool", "tool_name": "mcp_bulk_file_writer", "tool_arguments": {"files": [{"content": "import React, { useState, useEffect } from 'react';\nimport { motion } from 'framer-motion';\nimport { Check, X } from 'lucide-react';\nimport Mascot from '../components/Mascot';\n\nconst SortingGame = ({ onComplete, color }) => {\n  const [round, setRound] = useState(1);\n  const [numbers, setNumbers] = useState([]);\n  const [selectedOrder, setSelectedOrder] = useState([]);\n  const [feedback, setFeedback] = useState(null);\n  const [score, setScore] = useState(0);\n  const [timeLeft, setTimeLeft] = useState(60);\n  const [gameStarted, setGameStarted] = useState(false);\n\n  const totalRounds = 5;\n\n  useEffect(() => {\n    if (gameStarted && timeLeft > 0 && round <= totalRounds) {\n      const timer = setTimeout(() => setTimeLeft(timeLeft - 1), 1000);\n      return () => clearTimeout(timer);\n    } else if (timeLeft === 0 || round > totalRounds) {\n      finishGame();\n    }\n  }, [timeLeft, gameStarted, round]);\n\n  const generateNumbers = (count) => {\n    const nums = [];\n    while (nums.length < count) {\n      const num = Math.floor(Math.random() * 50) + 1;\n      if (!nums.includes(num)) nums.push(num);\n    }\n    return nums.sort(() => Math.random() - 0.5); // Shuffle\n  };\n\n  const startGame = () => {\n    setGameStarted(true);\n    startNewRound();\n  };\n\n  const startNewRound = () => {\n    const count = Math.min(3 + round, 7); // Start with 4, max 7 numbers\n    setNumbers(generateNumbers(count));\n    setSelectedOrder([]);\n    setFeedback(null);\n  };\n\n  const handleNumberClick = (num) => {\n    if (feedback || selectedOrder.includes(num)) return;\n    setSelectedOrder([...selectedOrder, num]);\n  };\n\n  const checkAnswer = () => {\n    const correctOrder = [...numbers].sort((a, b) => a - b);\n    const isCorrect = JSON.stringify(selectedOrder) === JSON.stringify(correctOrder);\n\n    if (isCorrect) {\n      setScore(score + 1);\n      setFeedback('correct');\n    } else {\n      setFeedback('wrong');\n    }\n\n    setTimeout(() => {\n      if (round < totalRounds) {\n        setRound(round + 1);\n        startNewRound();\n      } else {\n        finishGame();\n      }\n    }, 2000);\n  };\n\n  const finishGame = () => {\n    const percentage = (score / totalRounds) * 100;\n    const stars = percentage >= 80 ? 3 : percentage >= 50 ? 2 : 1;\n    onComplete({ stars, score, total: totalRounds });\n  };\n\n  if (!gameStarted) {\n    return (\n      <div className=\"space-y-8\" data-testid=\"sorting-game-start\">\n        <div className=\"bg-white rounded-[32px] p-8 shadow-xl\">\n          <h2 className=\"heading-font text-3xl text-[#2B2D42] mb-4\">Cara Bermain:</h2>\n          <ul className=\"body-font text-lg text-[#6C757D] space-y-2 list-disc list-inside\">\n            <li>Klik angka-angka sesuai urutan dari <strong>terkecil ke terbesar</strong></li>\n            <li>Ada {totalRounds} putaran</li>\n            <li>Waktu: 60 detik</li>\n            <li>Semakin banyak benar, semakin banyak bintang!</li>\n          </ul>\n        </div>\n        <div className=\"text-center\">\n          <button\n            data-testid=\"start-sorting-game\"\n            onClick={startGame}\n            className=\"bouncy-button px-12 py-6 rounded-full font-bold text-2xl text-white shadow-xl\"\n            style={{ backgroundColor: color }}\n          >\n            Mulai Permainan! \ud83c\udfae\n          </button>\n        </div>\n      </div>\n    );\n  }\n\n  return (\n    <div className=\"space-y-6\" data-testid=\"sorting-game-playing\">\n      {/* Score & Timer */}\n      <div className=\"bg-white rounded-[32px] p-6 shadow-lg flex justify-between items-center\">\n        <div>\n          <span className=\"heading-font text-2xl text-[#2B2D42]\">Putaran {round}/{totalRounds}</span>\n          <span className=\"body-font text-xl text-[#6C757D] ml-4\">Skor: {score}</span>\n        </div>\n        <div className=\"heading-font text-3xl\" style={{ color }}>\n          \u23f1\ufe0f {timeLeft}s\n        </div>\n      </div>\n\n      {/* Instructions */}\n      <div className=\"bg-white rounded-[32px] p-6 shadow-lg text-center\">\n        <p className=\"body-font text-xl text-[#2B2D42]\">\n          Klik angka dari <strong>terkecil ke terbesar!</strong>\n        </p>\n      </div>\n\n      {/* Numbers Grid */}\n      <div className=\"bg-white rounded-[32px] p-8 shadow-xl\">\n        <div className=\"grid grid-cols-3 sm:grid-cols-4 md:grid-cols-5 gap-4\">\n          {numbers.map((num) => {\n            const isSelected = selectedOrder.includes(num);\n            const selectedIndex = selectedOrder.indexOf(num);\n\n            return (\n              <motion.button\n                key={num}\n                data-testid={`number-${num}`}\n                onClick={() => handleNumberClick(num)}\n                disabled={isSelected || feedback}\n                className={`rounded-[24px] p-6 font-bold text-3xl shadow-lg transition-all ${\n                  isSelected\n                    ? 'opacity-50 cursor-not-allowed'\n                    : 'bouncy-button cursor-pointer'\n                }`}\n                style={{\n                  backgroundColor: isSelected ? '#E0E0E0' : color,\n                  color: 'white'\n                }}\n                whileHover={!isSelected && !feedback ? { scale: 1.1 } : {}}\n                whileTap={!isSelected && !feedback ? { scale: 0.95 } : {}}\n              >\n                <div>{num}</div>\n                {isSelected && (\n                  <div className=\"text-sm mt-1\">#{selectedIndex + 1}</div>\n                )}\n              </motion.button>\n            );\n          })}\n        </div>\n      </div>\n\n      {/* Selected Order Display */}\n      {selectedOrder.length > 0 && (\n        <div className=\"bg-white rounded-[32px] p-6 shadow-lg\">\n          <p className=\"body-font text-lg text-[#6C757D] mb-3\">Urutan yang kamu pilih:</p>\n          <div className=\"flex flex-wrap gap-3 justify-center\">\n            {selectedOrder.map((num, idx) => (\n              <div\n                key={idx}\n                className=\"px-6 py-3 rounded-full font-bold text-2xl\"\n                style={{ backgroundColor: color, color: 'white' }}\n              >\n                {num}\n              </div>\n            ))}\n          </div>\n        </div>\n      )}\n\n      {/* Check Button */}\n      {selectedOrder.length === numbers.length && !feedback && (\n        <div className=\"text-center\">\n          <motion.button\n            data-testid=\"check-answer-sorting\"\n            onClick={checkAnswer}\n            className=\"bouncy-button bg-[#FFD166] text-[#2B2D42] px-12 py-6 rounded-full font-bold text-2xl shadow-xl\"\n            initial={{ scale: 0 }}\n            animate={{ scale: 1 }}\n            transition={{ type: 'spring' }}\n          >\n            Cek Jawaban! \u2713\n          </motion.button>\n        </div>\n      )}\n\n      {/* Feedback */}\n      {feedback && (\n        <motion.div\n          initial={{ scale: 0 }}\n          animate={{ scale: 1 }}\n          className={`rounded-[32px] p-8 shadow-xl flex items-center justify-center gap-4 ${\n            feedback === 'correct' ? 'bg-[#6BCB77]' : 'bg-[#EF476F]'\n          }`}\n        >\n          <Mascot mood={feedback === 'correct' ? 'happy' : 'sad'} size=\"small\" />\n          <div className=\"text-white\">\n            {feedback === 'correct' ? (\n              <div className=\"flex items-center gap-3\">\n                <Check className=\"w-10 h-10\" />\n                <span className=\"heading-font text-3xl\">Benar! Hebat!</span>\n              </div>\n            ) : (\n              <div className=\"flex items-center gap-3\">\n                <X className=\"w-10 h-10\" />\n                <span className=\"heading-font text-3xl\">Coba lagi!</span>\n              </div>\n            )}\n          </div>\n        </motion.div>\n      )}\n    </div>\n  );\n};\n\nexport default SortingGame;\n", "path": "/app/frontend/src/games/SortingGame.js"}, {"content": "import React, { useState, useEffect } from 'react';\nimport { motion } from 'framer-motion';\nimport { Check, X } from 'lucide-react';\nimport Mascot from '../components/Mascot';\n\nconst PatternGame = ({ onComplete, color }) => {\n  const [round, setRound] = useState(1);\n  const [pattern, setPattern] = useState([]);\n  const [options, setOptions] = useState([]);\n  const [feedback, setFeedback] = useState(null);\n  const [score, setScore] = useState(0);\n  const [gameStarted, setGameStarted] = useState(false);\n\n  const totalRounds = 10;\n  const shapes = ['\ud83d\udd34', '\ud83d\udd35', '\ud83d\udfe2', '\ud83d\udfe1', '\ud83d\udfe3', '\ud83d\udfe0'];\n\n  const startGame = () => {\n    setGameStarted(true);\n    generatePattern();\n  };\n\n  const generatePattern = () => {\n    const patternLength = Math.min(3 + Math.floor(round / 2), 6);\n    const repeatCount = Math.floor(round / 3) + 2;\n    \n    // Create a base pattern\n    const basePattern = [];\n    for (let i = 0; i < patternLength; i++) {\n      basePattern.push(shapes[Math.floor(Math.random() * shapes.length)]);\n    }\n    \n    // Repeat it\n    const fullPattern = [];\n    for (let i = 0; i < repeatCount; i++) {\n      fullPattern.push(...basePattern);\n    }\n    \n    const correctAnswer = fullPattern[fullPattern.length % basePattern.length];\n    \n    setPattern(fullPattern.slice(0, -1)); // Remove last one for user to guess\n    \n    // Generate options\n    const opts = [correctAnswer];\n    while (opts.length < 4) {\n      const randomShape = shapes[Math.floor(Math.random() * shapes.length)];\n      if (!opts.includes(randomShape)) {\n        opts.push(randomShape);\n      }\n    }\n    \n    setOptions(opts.sort(() => Math.random() - 0.5));\n    setFeedback(null);\n  };\n\n  const handleAnswer = (selected) => {\n    // Find the correct answer\n    const patternLength = pattern.length;\n    let baseLength = 2;\n    \n    // Try to find repeating pattern\n    for (let len = 2; len <= Math.floor(patternLength / 2); len++) {\n      let isPattern = true;\n      for (let i = len; i < patternLength; i++) {\n        if (pattern[i] !== pattern[i % len]) {\n          isPattern = false;\n          break;\n        }\n      }\n      if (isPattern) {\n        baseLength = len;\n        break;\n      }\n    }\n    \n    const correctAnswer = pattern[patternLength % baseLength];\n    \n    if (selected === correctAnswer) {\n      setScore(score + 1);\n      setFeedback('correct');\n    } else {\n      setFeedback('wrong');\n    }\n\n    setTimeout(() => {\n      if (round < totalRounds) {\n        setRound(round + 1);\n        generatePattern();\n      } else {\n        finishGame();\n      }\n    }, 2000);\n  };\n\n  const finishGame = () => {\n    const percentage = (score / totalRounds) * 100;\n    const stars = percentage >= 70 ? 3 : percentage >= 50 ? 2 : 1;\n    onComplete({ stars, score, total: totalRounds });\n  };\n\n  if (!gameStarted) {\n    return (\n      <div className=\"space-y-8\" data-testid=\"pattern-game-start\">\n        <div className=\"bg-white rounded-[32px] p-8 shadow-xl\">\n          <h2 className=\"heading-font text-3xl text-[#2B2D42] mb-4\">Cara Bermain:</h2>\n          <ul className=\"body-font text-lg text-[#6C757D] space-y-2 list-disc list-inside\">\n            <li>Perhatikan urutan bentuk dan warna</li>\n            <li>Temukan <strong>pola yang berulang</strong></li>\n            <li>Pilih bentuk yang seharusnya muncul berikutnya</li>\n            <li>Ada {totalRounds} putaran</li>\n          </ul>\n        </div>\n        <div className=\"text-center\">\n          <button\n            data-testid=\"start-pattern-game\"\n            onClick={startGame}\n            className=\"bouncy-button px-12 py-6 rounded-full font-bold text-2xl text-white shadow-xl\"\n            style={{ backgroundColor: color }}\n          >\n            Mulai Permainan! \ud83c\udfae\n          </button>\n        </div>\n      </div>\n    );\n  }\n\n  return (\n    <div className=\"space-y-6\" data-testid=\"pattern-game-playing\">\n      {/* Score */}\n      <div className=\"bg-white rounded-[32px] p-6 shadow-lg flex justify-between items-center\">\n        <span className=\"heading-font text-2xl text-[#2B2D42]\">Putaran {round}/{totalRounds}</span>\n        <span className=\"body-font text-xl text-[#6C757D]\">Skor: {score}</span>\n      </div>\n\n      {/* Pattern Display */}\n      <div className=\"bg-white rounded-[32px] p-8 shadow-xl\">\n        <p className=\"body-font text-xl text-[#2B2D42] mb-6 text-center\">\n          Apa bentuk berikutnya?\n        </p>\n        <div className=\"flex flex-wrap justify-center gap-3 mb-4\">\n          {pattern.map((shape, idx) => (\n            <motion.div\n              key={idx}\n              className=\"text-5xl\"\n              initial={{ scale: 0 }}\n              animate={{ scale: 1 }}\n              transition={{ delay: idx * 0.1 }}\n            >\n              {shape}\n            </motion.div>\n          ))}\n          <div className=\"text-5xl font-bold text-[#2B2D42]\">\u2753</div>\n        </div>\n      </div>\n\n      {/* Options */}\n      {!feedback && (\n        <div className=\"bg-white rounded-[32px] p-8 shadow-xl\">\n          <p className=\"body-font text-lg text-[#6C757D] mb-6 text-center\">\n            Pilih jawaban:\n          </p>\n          <div className=\"grid grid-cols-2 sm:grid-cols-4 gap-4\">\n            {options.map((option, idx) => (\n              <motion.button\n                key={idx}\n                data-testid={`option-${idx}`}\n                onClick={() => handleAnswer(option)}\n                className=\"bouncy-button rounded-[24px] p-8 shadow-lg text-6xl\"\n                style={{ backgroundColor: color }}\n                whileHover={{ scale: 1.1 }}\n                whileTap={{ scale: 0.9 }}\n              >\n                {option}\n              </motion.button>\n            ))}\n          </div>\n        </div>\n      )}\n\n      {/* Feedback */}\n      {feedback && (\n        <motion.div\n          initial={{ scale: 0 }}\n          animate={{ scale: 1 }}\n          className={`rounded-[32px] p-8 shadow-xl flex items-center justify-center gap-4 ${\n            feedback === 'correct' ? 'bg-[#6BCB77]' : 'bg-[#EF476F]'\n          }`}\n        >\n          <Mascot mood={feedback === 'correct' ? 'happy' : 'sad'} size=\"small\" />\n          <div className=\"text-white\">\n            {feedback === 'correct' ? (\n              <div className=\"flex items-center gap-3\">\n                <Check className=\"w-10 h-10\" />\n                <span className=\"heading-font text-3xl\">Benar! Keren!</span>\n              </div>\n            ) : (\n              <div className=\"flex items-center gap-3\">\n                <X className=\"w-10 h-10\" />\n                <span className=\"heading-font text-3xl\">Hampir!</span>\n              </div>\n            )}\n          </div>\n        </motion.div>\n      )}\n    </div>\n  );\n};\n\nexport default PatternGame;\n", "path": "/app/frontend/src/games/PatternGame.js"}, {"content": "import React, { useState } from 'react';\nimport { motion } from 'framer-motion';\nimport { ArrowUp, ArrowDown, ArrowLeft, ArrowRight, Play, RotateCcw } from 'lucide-react';\nimport Mascot from '../components/Mascot';\n\nconst AlgorithmGame = ({ onComplete, color }) => {\n  const [level, setLevel] = useState(1);\n  const [commands, setCommands] = useState([]);\n  const [robotPos, setRobotPos] = useState({ x: 0, y: 0 });\n  const [targetPos, setTargetPos] = useState({ x: 3, y: 3 });\n  const [isRunning, setIsRunning] = useState(false);\n  const [score, setScore] = useState(0);\n  const [feedback, setFeedback] = useState(null);\n  const [gameStarted, setGameStarted] = useState(false);\n\n  const gridSize = 5;\n  const maxLevels = 5;\n\n  const directions = [\n    { icon: ArrowUp, value: 'up', label: '\u2191' },\n    { icon: ArrowDown, value: 'down', label: '\u2193' },\n    { icon: ArrowLeft, value: 'left', label: '\u2190' },\n    { icon: ArrowRight, value: 'right', label: '\u2192' }\n  ];\n\n  const startGame = () => {\n    setGameStarted(true);\n    generateLevel();\n  };\n\n  const generateLevel = () => {\n    // Random start and target positions\n    const startX = Math.floor(Math.random() * gridSize);\n    const startY = Math.floor(Math.random() * gridSize);\n    let targetX, targetY;\n    \n    do {\n      targetX = Math.floor(Math.random() * gridSize);\n      targetY = Math.floor(Math.random() * gridSize);\n    } while (startX === targetX && startY === targetY);\n    \n    setRobotPos({ x: startX, y: startY });\n    setTargetPos({ x: targetX, y: targetY });\n    setCommands([]);\n    setFeedback(null);\n  };\n\n  const addCommand = (direction) => {\n    if (commands.length < 15 && !isRunning) {\n      setCommands([...commands, direction]);\n    }\n  };\n\n  const removeLastCommand = () => {\n    if (!isRunning) {\n      setCommands(commands.slice(0, -1));\n    }\n  };\n\n  const clearCommands = () => {\n    if (!isRunning) {\n      setCommands([]);\n    }\n  };\n\n  const runCommands = async () => {\n    setIsRunning(true);\n    let currentPos = { ...robotPos };\n\n    for (let cmd of commands) {\n      await new Promise(resolve => setTimeout(resolve, 500));\n      \n      switch (cmd) {\n        case 'up':\n          if (currentPos.y > 0) currentPos.y -= 1;\n          break;\n        case 'down':\n          if (currentPos.y < gridSize - 1) currentPos.y += 1;\n          break;\n        case 'left':\n          if (currentPos.x > 0) currentPos.x -= 1;\n          break;\n        case 'right':\n          if (currentPos.x < gridSize - 1) currentPos.x += 1;\n          break;\n      }\n      \n      setRobotPos({ ...currentPos });\n    }\n\n    // Check if robot reached target\n    const success = currentPos.x === targetPos.x && currentPos.y === targetPos.y;\n    \n    if (success) {\n      setScore(score + 1);\n      setFeedback('correct');\n      \n      setTimeout(() => {\n        if (level < maxLevels) {\n          setLevel(level + 1);\n          generateLevel();\n          setIsRunning(false);\n        } else {\n          finishGame();\n        }\n      }, 2000);\n    } else {\n      setFeedback('wrong');\n      setTimeout(() => {\n        setRobotPos({ x: robotPos.x, y: robotPos.y }); // Reset to start\n        setIsRunning(false);\n        setFeedback(null);\n      }, 2000);\n    }\n  };\n\n  const finishGame = () => {\n    const percentage = (score / maxLevels) * 100;\n    const stars = percentage >= 80 ? 3 : percentage >= 60 ? 2 : 1;\n    onComplete({ stars, score, total: maxLevels });\n  };\n\n  if (!gameStarted) {\n    return (\n      <div className=\"space-y-8\" data-testid=\"algorithm-game-start\">\n        <div className=\"bg-white rounded-[32px] p-8 shadow-xl\">\n          <h2 className=\"heading-font text-3xl text-[#2B2D42] mb-4\">Cara Bermain:</h2>\n          <ul className=\"body-font text-lg text-[#6C757D] space-y-2 list-disc list-inside\">\n            <li>Buat <strong>urutan perintah</strong> untuk menggerakkan robot \ud83e\udd16</li>\n            <li>Gunakan tombol panah untuk memberi perintah</li>\n            <li>Robot harus mencapai target \u2b50</li>\n            <li>Klik \"Jalankan\" untuk melihat robot bergerak!</li>\n            <li>Ada {maxLevels} level</li>\n          </ul>\n        </div>\n        <div className=\"text-center\">\n          <button\n            data-testid=\"start-algorithm-game\"\n            onClick={startGame}\n            className=\"bouncy-button px-12 py-6 rounded-full font-bold text-2xl text-white shadow-xl\"\n            style={{ backgroundColor: color }}\n          >\n            Mulai Permainan! \ud83c\udfae\n          </button>\n        </div>\n      </div>\n    );\n  }\n\n  return (\n    <div className=\"space-y-6\" data-testid=\"algorithm-game-playing\">\n      {/* Score */}\n      <div className=\"bg-white rounded-[32px] p-6 shadow-lg flex justify-between items-center\">\n        <span className=\"heading-font text-2xl text-[#2B2D42]\">Level {level}/{maxLevels}</span>\n        <span className=\"body-font text-xl text-[#6C757D]\">Berhasil: {score}</span>\n      </div>\n\n      {/* Grid */}\n      <div className=\"bg-white rounded-[32px] p-8 shadow-xl\">\n        <p className=\"body-font text-xl text-[#2B2D42] mb-6 text-center\">\n          Pandu robot \ud83e\udd16 ke target \u2b50!\n        </p>\n        <div className=\"grid gap-2\" style={{ gridTemplateColumns: `repeat(${gridSize}, 1fr)` }}>\n          {Array.from({ length: gridSize * gridSize }).map((_, idx) => {\n            const x = idx % gridSize;\n            const y = Math.floor(idx / gridSize);\n            const isRobot = robotPos.x === x && robotPos.y === y;\n            const isTarget = targetPos.x === x && targetPos.y === y;\n\n            return (\n              <motion.div\n                key={idx}\n                className=\"aspect-square rounded-2xl border-4 border-gray-200 flex items-center justify-center text-4xl\"\n                style={{\n                  backgroundColor: isRobot || isTarget ? color : 'white'\n                }}\n                animate={isRobot ? { scale: [1, 1.1, 1] } : {}}\n                transition={{ duration: 0.5 }}\n              >\n                {isRobot && '\ud83e\udd16'}\n                {isTarget && '\u2b50'}\n              </motion.div>\n            );\n          })}\n        </div>\n      </div>\n\n      {/* Commands Display */}\n      <div className=\"bg-white rounded-[32px] p-6 shadow-xl\">\n        <p className=\"body-font text-lg text-[#6C757D] mb-3\">Perintah yang kamu buat:</p>\n        <div className=\"flex flex-wrap gap-2 min-h-[60px] items-center justify-center bg-gray-50 rounded-2xl p-4\">\n          {commands.length === 0 ? (\n            <span className=\"text-gray-400 body-font\">Tambahkan perintah...</span>\n          ) : (\n            commands.map((cmd, idx) => {\n              const dir = directions.find(d => d.value === cmd);\n              return (\n                <div\n                  key={idx}\n                  className=\"px-4 py-2 rounded-full font-bold text-xl\"\n                  style={{ backgroundColor: color, color: 'white' }}\n                >\n                  {dir.label}\n                </div>\n              );\n            })\n          )}\n        </div>\n      </div>\n\n      {/* Direction Buttons */}\n      <div className=\"bg-white rounded-[32px] p-6 shadow-xl\">\n        <div className=\"grid grid-cols-2 gap-4 mb-4\">\n          {directions.map((dir) => {\n            const Icon = dir.icon;\n            return (\n              <button\n                key={dir.value}\n                data-testid={`direction-${dir.value}`}\n                onClick={() => addCommand(dir.value)}\n                disabled={isRunning}\n                className=\"bouncy-button rounded-2xl p-6 text-white font-bold text-xl flex items-center justify-center gap-2 disabled:opacity-50\"\n                style={{ backgroundColor: color }}\n              >\n                <Icon className=\"w-8 h-8\" />\n                {dir.label}\n              </button>\n            );\n          })}\n        </div>\n\n        <div className=\"grid grid-cols-3 gap-4\">\n          <button\n            data-testid=\"clear-commands\"\n            onClick={clearCommands}\n            disabled={isRunning || commands.length === 0}\n            className=\"bouncy-button bg-gray-200 text-[#2B2D42] rounded-2xl p-4 font-bold disabled:opacity-50\"\n          >\n            <RotateCcw className=\"w-6 h-6 mx-auto\" />\n            Hapus\n          </button>\n          <button\n            data-testid=\"remove-last-command\"\n            onClick={removeLastCommand}\n            disabled={isRunning || commands.length === 0}\n            className=\"bouncy-button bg-gray-200 text-[#2B2D42] rounded-2xl p-4 font-bold disabled:opacity-50\"\n          >\n            \u2190 Batal\n          </button>\n          <button\n            data-testid=\"run-commands\"\n            onClick={runCommands}\n            disabled={isRunning || commands.length === 0}\n            className=\"bouncy-button bg-[#6BCB77] text-white rounded-2xl p-4 font-bold text-lg flex items-center justify-center gap-2 disabled:opacity-50\"\n          >\n            <Play className=\"w-6 h-6\" />\n            Jalankan!\n          </button>\n        </div>\n      </div>\n\n      {/* Feedback */}\n      {feedback && (\n        <motion.div\n          initial={{ scale: 0 }}\n          animate={{ scale: 1 }}\n          className={`rounded-[32px] p-8 shadow-xl flex items-center justify-center gap-4 ${\n            feedback === 'correct' ? 'bg-[#6BCB77]' : 'bg-[#EF476F]'\n          }`}\n        >\n          <Mascot mood={feedback === 'correct' ? 'happy' : 'sad'} size=\"small\" />\n          <span className=\"heading-font text-3xl text-white\">\n            {feedback === 'correct' ? 'Berhasil! Hebat!' : 'Belum sampai! Coba lagi!'}\n          </span>\n        </motion.div>\n      )}\n    </div>\n  );\n};\n\nexport default AlgorithmGame;\n", "path": "/app/frontend/src/games/AlgorithmGame.js"}]}}
Observation: Successfully wrote 3 files:
- /app/frontend/src/games/SortingGame.js
- /app/frontend/src/games/PatternGame.js
- /app/frontend/src/games/AlgorithmGame.js